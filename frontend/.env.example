# URL de la API. Por defecto apunta al contenedor publicado en local.
VITE_API_URL=http://localhost:8000

# Clave del panel. Debe coincidir con PANEL_API_KEY del docker-compose.
# Si esa variable esta vacia en el backend, esta tambien puede quedar vacia.
VITE_API_KEY=
